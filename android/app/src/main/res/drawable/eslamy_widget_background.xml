<?xml version="1.0" encoding="utf-8"?>
<!-- Mirrors the in-app PrayerTimesCard hero gradient (AppColors.heroGradient),
     so the home-screen widget reads as the same brand surface as the app. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="24dp" />
    <gradient
        android:angle="315"
        android:startColor="#0E7C60"
        android:endColor="#1A9B76" />
</shape>
