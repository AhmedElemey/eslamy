<?xml version="1.0" encoding="utf-8"?>
<!-- Highlights the current/next prayer's time in the schedule row of
     eslamy_widget_prayer_layout.xml — dark teal fill (AppColors.
     primaryDeepest) with white text, the same "dark pill on a light card"
     pattern as the reference design, in the app's own brand color instead
     of a generic charcoal. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="10dp" />
    <solid android:color="#0B3D32" />
</shape>
