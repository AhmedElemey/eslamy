<?xml version="1.0" encoding="utf-8"?>
<!-- Light card background for the Prayer widget layout only — matches the
     light/white reference design (as opposed to the teal gradient used by
     eslamy_widget_layout.xml for Ayah/Dua/Hijri). Uses the app's own
     parchment surface + hairline tokens (AppColors.surfaceLight /
     hairlineLight) rather than a generic gray, so it still reads as Eslamy. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="24dp" />
    <solid android:color="#FFFCF6" />
    <stroke
        android:width="1dp"
        android:color="#E6DFD2" />
</shape>
